<%= form_with(model: movie) do |form| %>
  <% if movie.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(movie.errors.count, "error") %> prohibited this movie from being saved:</h2>

      <ul>
        <% movie.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

<table class="table">
  <tr>
    <td><%= form.label :name %></td>
    <td><%= form.text_field :name %></td>
  </tr>
  <tr>
    <td><%= form.label :client_id%></td> 
    <td><%= form.select :client_id, options_for_select(@clients) %></td>   
  </tr>
  <tr>
    <td class="text-center"colspan="3"><%= form.submit "Validate",style:"font-weight:bold;background-color: #043c4b;border-radius:15px;color:#f4ede6 ;margin:5px;padding:5px"%></td>
  </tr>
</table>
<% end %>
